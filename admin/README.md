# Admin

Next.js 15 + TS + Tailwind + shadcn (skeleton to be added).
EOF
cat > package.json <<\"EOF\"
{
  "name": "admin",
  "private": true,
  "scripts": {
    "dev": "echo Add Next.js app here",
    "build": "echo Add Next.js app here"
  }
}
EOF
